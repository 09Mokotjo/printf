#include <stdio.h>

/**
 * main - entry point of the program
 *
 * This function is an entry point of the program. It calls the
 * printReversedString function to demonstrate printing  reversed
 * version of a string.
 *
 * Return: 0 on successful execution
 */
int main(void) {
    const char* text = "Hello, World!";
    printReversedString(text);

    return 0;
}

